<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h5 class="text-xl mb-3">Edit your post here</h5>
      <form method="post" action="/update/<%= post._id %>">
        <textarea
          placeholder="<%= post.content %>"
          class="resize-none bg-transparent border-2 border-zinc-800 outline-none p-3 w-1/3 rounded-md"
          name="content"
        ></textarea>
        <input
          class="bg-yellow-500 font-bold text-black px-3 py-2 w-36 rounded-md mt-1 block text-sm"
          type="submit"
          value="Update post"
        />
      </form>
    </div>
  </body>
</html>
